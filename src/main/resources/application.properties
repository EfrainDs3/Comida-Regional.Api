# ==========================================
# CONFIGURACIÓN GENERAL
# ==========================================
spring.application.name=Regional-Api
server.port=2044

# ==========================================
# BASE DE DATOS (MySQL)
# ==========================================
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/rest?useSSL=false&serverTimezone=America/Lima&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=


# Credenciales antiguas (Respaldo)
# spring.datasource.username=comidas_regional
# spring.datasource.password=.restful2025-

# ==========================================
# JPA / HIBERNATE
# ==========================================
# Muestra las consultas SQL en consola (útil para depurar)
spring.jpa.show-sql=true

# Desactiva el warning de rendimiento "Open in View"
spring.jpa.open-in-view=false

# Estrategias de nombrado (Opcional, por defecto suele estar bien)
# spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# COMENTADO: Spring Boot detecta el dialecto automáticamente (MySQL 8). 
# Descomentar esto causaba el warning de versión "5.5.5".
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ==========================================
# SEGURIDAD (Spring Security)
# ==========================================
# Usuario y clave fijos para desarrollo (evita la clave aleatoria en consola)
spring.security.user.name=admin
spring.security.user.password=admin123

# ==========================================
# EMAIL (Gmail SMTP)
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=anllyriva14@gmail.com
# Contraseña de aplicación (App Password)
spring.mail.password=trtt hrbg zejv xhxy 
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ==========================================
# CONFIGURACIÓN SSL (Https) - NO USADO
# ==========================================
# server.ssl.enabled=true
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=changeit
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=regional-api